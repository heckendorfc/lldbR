#######################################################

PKG_LIBS = @LLDBR_LIBS@
OBEXTRA = @OBEXTRA@
LLDB_HOME = @LLDB_HOME@

LLDBOBJ = lldb/clldb.a
ROBJ = raw.o lldbtypes.o
OBJECTS = $(ROBJ) $(LLDBOBJ)

all: $(SHLIB)


$(SHLIB): $(OBJECTS)
$(OBJECTS): $(OBEXTRA)


mkclldb:
	( mkdir -p libs; cd lldb; gmake BUILD_DIR=../ LLDB_HOME=$(LLDB_HOME) LDB_LIBS=$(PKG_LIBS))

instclldb: mkclldb

clean:
	rm -rf ./libs ./*.so ./*.o
